import React, {
  Component,
  PropTypes
} from 'react';
export default class GameApp extends Component {
  hiddenLoaingView() {
    this.setState({
    })
  }
  render() {
    return ( <div  className = "container" ></div>);
  };
}
